# Legacy patch scripts

These scripts were part of the iterative development history of the application.
They are intentionally kept outside the application source so the project root
and runtime remain clean, while preserving the scripts for reference/recovery.

They are **not part of the build** and should not be used as the application's
runtime architecture.
