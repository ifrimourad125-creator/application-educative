# V16 — lecture visuals

The Lecture activity now renders the unit's own `reading.image` and `reading.text` instead of a generic
level-based illustration/text fallback.

Referenced reading image assets: 11
Assets currently present: 11
Assets still needing generated files: 0

Missing paths:


The UI uses the same visual container and styling already present in the application.
